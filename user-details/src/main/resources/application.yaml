server:
  port: 8081

spring:
  application:
    name: user-details

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/users_details
    username: root
    password: users_db
    driver-class-name: com.mysql.cj.jdbc.Driver